# See _core.pyx.template for C++ declarations. This module is intended to be built with Cython.
# The companion setup.py compiles the real binding and exposes pandas DataFrame outputs.
# Direct API documented in README.md and TUTORIAL.md.
